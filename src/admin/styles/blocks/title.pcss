.login__form-title {
    font-size: 36px;
}

.title {
    font-size: 21px;
    font-weight: bold;
    color:$text-color;
    &--about {
        margin-right: 60px;
    }
}

.edit-block__title {
    font-size: 18px;
  font-weight: bold;
  border-bottom: 1px solid rgba(#1f232d, 0.2);
  padding-bottom: 25px;
  padding-left: 10px;
  margin-bottom: 50px;

  @include tablets {
    margin-bottom: 4%;
  }
}